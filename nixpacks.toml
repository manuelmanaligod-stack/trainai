[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.requests"]

[phases.build]
cmds = ["pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "python server_v8.py"
